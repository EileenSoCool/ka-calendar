---
name: ka-calendar
description: 王栎鑫行程月历（𝐊.𝐀月历）制作与修订管线。当用户要求抓取微博、整理或修改王栎鑫某月行程/活动/播出/物料汇总月历时使用。覆盖授权账号清单、低风控慢速抓取、转发链溯源到原博、骨架完备性自检、按淘金版式组装纯文本月历。
---

# K.A 月历制作管线

目标：把某年某月王栎鑫的公开行程/演出/节目播出/新歌物料，整理成一份粉丝可直接发布微博的链接月历文本。

## 总流程（8 步）

1. **确认目标月份与 Cookie**：向用户确认年月，按 `references/cookie-guide.md` 教程引导用户提供当季有效的微博 `SUB` Cookie。**绝不把 cookie 写入持久文件/记忆**。
2. **全量抓取**：近月份（≤3 个月前）走 `scripts/month_mega.py` 后台慢速翻页；**3 个月前的老月份**走 `scripts/month_fastjump.py`——先深跳探针（p40/p80/p120）看最早日期，再二分定位到目标月起始页，从该页往下翻，大幅提速（用户 2026-09-10 教的方法）。账号清单永远以 `references/sources.md` 为准——**不要再幻想编造账号**。
3. **同步博配对**（`scripts/match_sync.py`）：王栎鑫当月每条原创 ↔ 同步博镜像（正文前 20 字归一匹配），产出 `sync_map`；语音帖注意同步博会拆两条镜像（图文+语音），合并展示。**半年可见期外的月份本人原博已隐藏，`mat_1292500037.json` 为空，本步产出空 sync_map 属正常**——此时本人博直接从 `sync.json` 取，第 8 步的自动校验退化为人工对照 sync.json。
4. **转发链溯源**（`scripts/extract_candidates.py`）：转发源全量帖子（账号以 `references/sources.md` 为准，含流水转发源 6 个 + caesocute）按王栎鑫相关关键词过滤；凡转发一律用 `retweeted_status` 的 uid/id 挂**原博主**链接（溯源纪律见下）。按日分组成 `candidates.txt`。
5. **通读全部 candidates 定事件骨架**：先看直接源（王/室/汇总号/后援会）确定主线事件，再**完整通读 candidates.txt** 补物料/直拍/cut/机场路透。**不允许只看直接源就动手**（8 月缺漏全是这么来的）。
6. **按淘金版式组装**（规则全在 `references/format-rules.md`，含"验收易错清单"，6 月已确认范例 `references/june_final.txt`），产出 `{month}_calendar.txt`。
7. **折叠单链接条目**（`scripts/collapse_single_link_items.py <单个成品txt>`）：**只对指定的单个成品文件跑，绝不整目录批跑**（批跑会重排用户已手改好的其他月份）。单链接小项压成 `1️⃣标题+链接` 一行；💌 双链（本人+同步博）保持两行不动。
8. **三件套自检**（全部必跑，缺一不交付）：
   - `scripts/check_calendar.py <成品txt>` — 格式 lint：半角日期、日期乱序、编号断裂、链接被空格切断、链接格式异常、同步博缺💌、标题链接粘连。
   - `scripts/check_skeleton.py <{month}_out目录> <成品txt>` — 骨架倒查：本人/工作室必收项是否缺失；带排除规则层（赏栎指南/打榜安利/转发隐藏博自动标记合法未收），跨月条目自动去其他月份成品找归属；输出"疑似真漏/待人工判断/跨月/合法未收"四分类。
   - `scripts/check_sync_links.py <成品txt> <sync_map.json>` — 同步博链完整性（半年可见期外月份 sync_map 为空，此步退化为人工对照 sync.json）。

## 骨架完备性自检（交付前强制逐条核对，缺一即返工）

> 血泪教训：1 月最早一版漏了工作室微博、同步博备份、机场全量路透、"大宝小宝"生日博等大量**骨架内容**，只堆了节目 cut。原因是流程第 8 步只查同步博链、不查骨架。本清单补齐这个机制，**组装完成后、交付用户前必须逐条跑一遍**。

骨架 = 六类主线链路，每类都要覆盖当月全部：

1. **本人链路（王栎鑫 1292500037）**：当月**每一条**原创博都要进月历，标 💌，后紧跟同步博备份链。
   - 易漏：生日博、节庆祝福（高考/建党/国庆）、日常打卡、**只有话题没有正文字的空博**（如 5 月"今天回家了"）。这些不是"小事"，都是骨架。
2. **工作室链路（5934487143）**：当月**每一条**工作室博都要进（鑫室·/鑫想室成·）。
   - 易漏：**预告类必全收**（封面预告/Teaser/上线预告/幕后vlog），`format-rules.md` 第 1 条点名。曾经"早安做客博""《Flowers》拍摄花絮"这类单条工作室博最容易被当成琐碎跳过。
3. **同步博链路（7796348707）**：王栎鑫每条原创博紧跟同步博备份链（他半年可见的对策），**链前必须标 💌**。
   - 易漏：同步博链前半角漏 💌 的 emoji；同步博一条镜像多帖（尤其 2 月春节密集）时对应关系要核对、日期留给用户判断。
4. **机场/上下班（五个源）**：当月的出发/到达/上下班/路透，五个源全量扫全（sources.md「机场/上下班」名单）。
   - 易漏：只挂 1 个机场源就收工；某源当天发多条只收 1 条。**每个源该日子的全量路透都要铺上**。
5. **节目 cut（cut 五源：大佬的走姿/猪oo猪zzz/吃水果捞/月亮/caesocute，另加汇总号）**：节目播出日按日聚合该期 cut；加更/彩蛋/未播也收（format-rules 第 12/13/14 条）。
6. **官方/汇总号**：演出当天必有「饭拍汇总」；官宣/首播/上线走官方账号名（天赐官方、汽水Live、QQ音乐、栎迷会……）。

**交付自查动作**：
- 对照 `sources.md` 账号清单，逐类确认每条链路在成品里都有对应条目。
- 尤其倒查当月王栎鑫**本人**所有原创博 id，逐条确认已在月历中出现 + 有 💌 + 有同步博链。
- 完成后再让用户手改；不要"只堆 cut"当成交付。

## 溯源纪律（最高优先，曾被用户纠正过）

- 链接必须解析到**最终原博主**。苦瓜等转发源只当"线索导航"：发现物料 → 读 `retweeted_status.user.id` + `retweeted_status.idstr` → 挂原博主的 `https://weibo.com/{uid}/{idstr}`。
- base62 短链（如 `R4xlwbC6B`）直接用 `weibo.com/ajax/statuses/show?id=<mid>` 查证（接口原生支持 base62），辅助脚本 `scripts/mid_lookup.py`，**不要自研 base62→id 转换**（实测手写算法会错）。
- 实在追不到原博才允许标「存档」挂中转链。
- **顺藤摸瓜发现新账号时，先问用户批准再纳入**（用户明确要求的规则 B）。

## 风控纪律（曾被用户点名别触发警告）

- 页间 ≥9s、账号间 ≥15s，报错退避 20s 重试 3 次；全程后台任务，禁止并发多开。
- 只用 `weibo.com/ajax/statuses/mymblog`（登录态可行），**不要碰** `m.weibo.cn`（免登录 432）与访客 cookie（mymblog 一律 403）。

## 工作流与产物存放（下次改月历先读这里，别重新抓）

### 成品 vs 抓取产物，是两套不同位置

1. **成品月历**：`~/Desktop/KA-calendar/2026-{MM}.txt`（用户直接发微博的最终文本）。
   - 同目录下 `{month}_calendar.original.txt`（如 `jan_calendar.original.txt`）是历史中间版本，**不是用户简版**，不要当他们是对照基准。

2. **抓取产物（中间数据）**：`~/{jan|feb|mar|apr|may|aug}_out/` 目录，每个目录结构一模一样：
   - `sync.json` —— **同步博（7796348707）的全量帖**。⚠️ 关键：王栎鑫本人（1292500037）半年可见已隐藏，`mat_1292500037.json` 常常是空 `[]`，**本人原创博要从 sync.json 里取**。
   - `mat_{uid}.json` —— 各直接源（工作室 5934487143、栎迷会 5691255041、汇总号 7868703091、栎亮鑫球 2796315530、本人 1292500037）。
   - `air_{uid}.json` —— 机场/上下班源全量。
   - `src_{uid}.json` —— 流水转发源全量（可能为空 `[]`，说明当时没抓到，别硬凑）。
   - `candidates.txt` —— 5 个转发源的帖子按日分组、已溯源到原博主的候选物料（节目 cut 主要从这取）。
   - `sync_map.json` —— 王栎鑫本人 x 同步博配对结果。

### 修改 / 重做某月的标准动作

1. 先 `ls ~/{month}_out/`，确认抓取产物还在。
2. 直接读 `sync.json` + `mat_*.json` + `air_*.json` + `candidates.txt`，**这些就是权威数据，不需要重新抓微博**。
3. 用骨架完备性自检清单（见上）逐条核对，组装成品。
4. 写回 `~/Desktop/KA-calendar/2026-{MM}.txt` 后，**必跑三件套自检**：`check_calendar.py`（格式 lint）+ `check_skeleton.py`（骨架倒查）+ `check_sync_links.py`（同步博链），全绿才交付用户手改。

### 血泪警告

- **永远不要用正则批量给链接"加空格"**——会切开链接 id 末位数字（如 `...3864940 3`）。链接只能整段保留，格式修正用 Write 重写整文件。
- 半年可见期规则：超过半年（回填 1/2 月等），本人原博已隐藏，💌 条目**只挂同步博**，不挂 1292500037。
- 抓取产物目录缺 `jun_out`/`jul_out`（6/7 月当时可能没建），要重做 6/7 月得先补抓。

## 关键参考文件

- `references/sources.md` — 全部已批准账号（主体/机场/转发源/官方/粉丝账号/黑名单）
- `references/cookie-guide.md` — 引导用户提供 SUB 的教程（流程开始先发给用户）
- `references/format-rules.md` — 淘金版式规则（结构、五类标签、去重、描述措辞、💌与同步博）
- `references/june_final.txt` — 用户亲手改定的 6 月成品（摹写标杆）
- `scripts/month_mega.py` — 可参数化月抓取
- `scripts/extract_candidates.py` — 转发源关键词提取+原博溯源
- `scripts/match_sync.py` — 王栎鑫×同步博配对
- `scripts/mid_lookup.py` — base62 mid 查证（走 statuses/show，无需转换）
- `scripts/month_fastjump.py` — 老月份深跳+二分定位抓取（先探针再二分起点页）
- `scripts/collapse_single_link_items.py` — 单链接小项折叠成一行（**只接受单个文件参数**）
- `scripts/check_sync_links.py` — 同步博链完整性自检
- `scripts/check_calendar.py` — 成品格式 lint（日期/编号/链接完整性/💌）
- `scripts/check_skeleton.py` — 骨架完备性倒查（带排除规则层 + 跨月查找）
